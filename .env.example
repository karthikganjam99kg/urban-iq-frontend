# ---------- Frontend (Vercel / .env.local) ----------
# Public backend URL (Hugging Face Space).
VITE_API_BASE_URL=https://priyaredddy-cse-hyderabad-urban-intelligence-api.hf.space

# Supabase project. The publishable key is browser-safe; RLS guards the tables.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# ---------- Backend (Hugging Face Space secret / local .env) ----------
# Billable key: keep it server-side only, never in the Vite bundle.
TOMTOM_API_KEY=
